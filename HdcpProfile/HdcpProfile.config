set (autostart ${PLUGIN_HDCPPROFILE_AUTOSTART})
set (preconditions Platform)
set (callsign "org.rdk.HdcpProfile")
map()
    key(root)
    map()
      kv(outofprocess ${PLUGIN_HDCPPROFILE_OUTOFPROCESS})
    end()
end()

ans(configuration)